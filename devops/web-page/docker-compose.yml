version: '3.8'

services:
  nginx:
    image: nginx:latest
    container_name: hakim_web
    ports:
      - "8090:80"
    volumes:
      - ./hakim.conf:/etc/nginx/conf.d/default.conf
      - ./html:/usr/share/nginx/html
    restart: unless-stopped
